<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>
    <groupId>com.portfolio</groupId>
    <artifactId>order-service</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <properties>
        <java.version>21</java.version>
        <pact.version>4.6.7</pact.version>
        <pactbroker.url>http://localhost:9292</pactbroker.url>
        <pactbroker.username>pact</pactbroker.username>
        <pactbroker.password>pact</pactbroker.password>
        <pact.consumer.version>0.0.1-local</pact.consumer.version>
        <pact.consumer.tag>local</pact.consumer.tag>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>au.com.dius.pact.consumer</groupId>
            <artifactId>junit5</artifactId>
            <version>${pact.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>au.com.dius.pact.provider</groupId>
                <artifactId>maven</artifactId>
                <version>${pact.version}</version>
                <configuration>
                    <pactBrokerUrl>${pactbroker.url}</pactBrokerUrl>
                    <pactBrokerUsername>${pactbroker.username}</pactBrokerUsername>
                    <pactBrokerPassword>${pactbroker.password}</pactBrokerPassword>
                    <projectVersion>${pact.consumer.version}</projectVersion>
                    <trimSnapshot>true</trimSnapshot>
                    <tags>
                        <tag>${pact.consumer.tag}</tag>
                    </tags>
                    <pactDirectory>${project.build.directory}/pacts</pactDirectory>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
